## Commands

```bash
pnpm run dev              # Dev + HMR
pnpm run build            # Prod build
pnpm test                 # All tests
rtk find <pattern>        # Search codebase (faster than grep)
rtk grep <pattern>        # Grep codebase (slower than rtk find)
rtk ls <pattern>          # List files (faster than find)
```
